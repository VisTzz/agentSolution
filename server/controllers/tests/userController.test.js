const userController = require('../userController')

describe("user controller", () => {
    describe("login", () => {
        it("success", async () => {
            expect('true').toBe('true');
        })        
    })
})